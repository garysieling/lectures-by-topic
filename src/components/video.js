import React from 'react'
import { Link } from 'gatsby'

const Video = ({  }) => (
  <div
    style={{
      background: 'blue',
      marginBottom: '1.45rem',
    }}
  >
      Video
  </div>
)

export default Video
